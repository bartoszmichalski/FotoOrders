{% extends 'base.html.twig' %}

{% block body %}
    <h1>Discountcoupon</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ discountCoupon.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ discountCoupon.code }}</td>
            </tr>
            <tr>
                <th>Used</th>
                <td>{% if discountCoupon.used %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('discountcoupon_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('discountcoupon_edit', { 'id': discountCoupon.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
