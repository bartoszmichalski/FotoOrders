{% extends 'base.html.twig' %}

{% block body %}
    <h1>Users list</h1>

    <table class="table table-hover col-md-offset-1">
        <thead>
            <tr>
                <th>User Name</th>
                <th>Email</th>
                <th>Last Login</th>
                <th>Enabled</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.username }}</td>
                <td>
                        <a href="{{ path('commission_allByUser_index', {'userId': user.id }) }}">
                            {{ user.email }}
                        </a>
                </td>
                <td>{{ user.lastLogin|date("Y/m/d")  }}</td>
                <td>{% if user.enabled == 1 %}Yes{% else %}No{% endif %}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>    
{% endblock %}
